rules_version = '2';

// Cloud Functions は Admin SDK 経由でアクセスするため、
// クライアントからの直接アクセスはすべて拒否する。
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
